> ⚠️ **ATENÇÃO:**  
> Os **diretórios de leitura e salvamento de arquivos CSV** estão definidos com caminhos absolutos.  
> Cada colaborador DEVE **REVISAR e ajustar os CAMINHOS locais** antes de executar o notebook.




Adicionando scripts .py relacionados á Machine Learning.

preprocess.py
Faz o pré processamento do csv bruto, normalizar e padroniza colunas, bem como define a coluna 'CHURN' como eixo X

----------

predict_churn_xgboost.py
Importa o modelo treinado e testa no dataset ja processado.

----------

plot_model.py
Plota 3 graficos para a visualização.

----------

Métricas:
Accuracy: 0.9505
F1-score: 0.7677
Recall: 0.8421



------------------------------------------------------------------------------------

Adicionando scripts de inserção do banco de dados.

insert_churn.py
Insere o dados do dataset de churn no banco de dados, cada linha está relacionada a uma coluna chamada empresa_id.

----------

insert_empresas.py
Insere do dados do csv das empresas no banco de dados.

----------

test.conn.py
Testa a conexão com o banco de dados.